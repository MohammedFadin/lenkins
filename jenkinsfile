pipeline {
    agnet { docker {image'node:6.3'}}
    stages {
        stage('build') {
            steps {
                sh 'echo "build stage started"'
                sh 'npm --version'
            }
        }
    }
}